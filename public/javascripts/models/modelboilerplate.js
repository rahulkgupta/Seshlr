define([
  'underscore',
  'backbone'
	'now'
], function(_, Backbone){
	var ??? = Backbone.Model.extend({
		//urlRoot: ONLY IF model is not part of a collection
	});
  return (new) ???; //new only if the model is not part of a collection
});
